import { ShieldCheck, Wrench } from "lucide-react";
import Image from "next/image";

import { RegisterForm } from "@/components/auth/register-form";

export default async function RegisterPage({ searchParams }: { searchParams: Promise<{ next?: string }> }) {
  const { next } = await searchParams;
  return <main className="relative min-h-dvh overflow-hidden bg-[#171a1f]"><Image src="/vehicle-service.jpg" alt="Vehículo listo para servicio" fill priority className="object-cover object-[65%_center]" sizes="100vw" /><div className="absolute inset-0 bg-black/70" /><div className="relative mx-auto grid min-h-dvh w-full max-w-7xl items-center gap-10 px-4 py-10 sm:px-8 lg:grid-cols-[minmax(0,1fr)_500px] lg:px-12"><section className="hidden max-w-xl text-white lg:block"><div className="flex items-center gap-3"><span className="grid size-12 place-items-center rounded-md bg-[#ef3f26]"><Wrench /></span><span className="font-display text-3xl font-bold">TorkeOS</span></div><h1 className="mt-10 font-display text-6xl font-bold leading-none">Tu historial mecánico empieza aquí.</h1><p className="mt-5 max-w-md text-lg leading-7 text-stone-200">Agenda en tu taller, sigue el diagnóstico y decide presupuestos con total claridad.</p><p className="mt-8 flex items-center gap-2 text-sm font-semibold text-emerald-300"><ShieldCheck size={20} /> Tus datos y vehículos permanecen en tu cuenta.</p></section><section className="mx-auto w-full max-w-lg rounded-lg border border-white/20 bg-white/95 p-6 shadow-2xl backdrop-blur sm:p-8"><div className="flex items-center gap-3 lg:hidden"><span className="grid size-11 place-items-center rounded-md bg-[#ef3f26] text-white"><Wrench /></span><span className="font-display text-2xl font-bold">TorkeOS</span></div><p className="text-xs font-bold uppercase text-[#d9341d] lg:mt-0">Portal del cliente</p><h2 className="mt-2 font-display text-4xl font-bold">Crea tu cuenta</h2><p className="mt-2 text-sm leading-6 text-stone-600">Registra tus vehículos, agenda citas y decide tus presupuestos.</p><RegisterForm nextPath={next} /></section></div></main>;
}